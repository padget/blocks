#ifndef __blocks_deserializer_hpp__
#define __blocks_deserializer_hpp__

namespace blocks
{
	template<typename type>
		struct simple_command_format
		{
			type read(const std::string& filename)
			{
				
			}
		};
	template<typename format>
		struct inplace_reader
}

#endif
